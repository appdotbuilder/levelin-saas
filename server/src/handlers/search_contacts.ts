
import { type Contact } from '../schema';

export const searchContacts = async (agencyId: number, query: string): Promise<Contact[]> => {
  // This is a placeholder declaration! Real code should be implemented here.
  // The goal of this handler is implementing full-text search across contacts
  // for the global search functionality in the top-bar.
  return [];
};
